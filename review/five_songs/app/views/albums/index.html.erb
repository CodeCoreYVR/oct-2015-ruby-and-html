<h1>All Albums</h1>

<table>
  <tr>
    <th>album title</th>
    <th>add song</th>
  </tr>
  <% @album.each do |album| %>
    <tr>
      <td><%= link_to album.title, album_path(album) %></td>
      <td><%= link_to "add song", "" %></td>
    </tr>
  <% end %>
</table>
